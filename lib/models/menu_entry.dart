class MenuEntry{
  int  numReviews;
  String name, section, id, restaurant_id,;
  double rating, price;
  MenuEntry({this.id, this.restaurant_id, this.numReviews, this.name, this.section, this.rating, this.price});
}